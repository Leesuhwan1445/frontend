const a = 1;
const b = 10;
const c = 100;

function funtionname(x, y) {
    return x + y;
}

console.log(funtionname(a, b))

//화살표 함수

const arrowfuntion = (x, y) => {
    return x + y
}
console.log(funtionname(a - c))

